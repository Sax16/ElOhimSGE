**Tag** — removable chip for active filters and multi-select tokens. `Badge` is for read-only status; `Tag` is interactive.

```jsx
<Tag onRemove={()=>…}>3° Primaria</Tag>
<Tag onClick={toggle} selected={on} leadingDot color="var(--gold-500)">Becados</Tag>
```

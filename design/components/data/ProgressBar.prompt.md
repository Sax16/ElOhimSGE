**ProgressBar** — ratio indicator for attendance, collection progress, grade-entry completion.

```jsx
<ProgressBar value={87} label="Asistencia" showValue tone="success" />
<ProgressBar value={62} max={100} tone="warning" striped />
```

Tones: brand·success·warning·danger·accent. Sizes: sm·md·lg.
